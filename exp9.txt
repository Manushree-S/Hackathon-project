
mkdir Exp9
cd Exp9

git init

echo "Version 1" > file.txt
git add .
git commit -m "Commit 1"

git remote add origin https://github.com/Manushree-S/Exp9.git
git branch -M main
git push -u origin main

git checkout -b source-branch

echo "Version 2" >> file.txt
git add .
git commit -m "Commit 2"

echo "Version 3" >> file.txt
git add .
git commit -m "Commit 3"

echo "Version 4" >> file.txt
git add .
git commit -m "Commit 4"

git log --oneline

git checkout main

# Replace the hashes below with your own commit IDs
git cherry-pick <Commit2-Hash>^..<Commit4-Hash>

git log --oneline
                Or
# Example of cherry-picking non-consecutive commits
git cherry-pick <Commit2-Hash> <Commit4-Hash>

git push origin main
