
mkdir Exp7
cd Exp7

git init

echo "Version 1" > file.txt
git add .
git commit -m "Commit 1"

git remote add origin https://github.com/Manushree-S/Exp7.git
git branch -M main
git push -u origin main

echo "Work in Progress" >> file.txt
git stash
git stash list
git stash pop

git add .
git commit -m "Commit 2"

echo "Version 3" >> file.txt
git add .
git commit -m "Commit 3"

git log --oneline
git rebase -i HEAD~2

git tag v1.0
git tag -a v2.0 -m "Release Version 2.0"
git tag
git show v2.0
git push origin --tags

echo "Temporary Change" >> file.txt
git restore file.txt

echo "Testing Revert" >> file.txt
git add .
git commit -m "Testing Revert"

git revert HEAD
git push origin main
